- we know the computers use only 2 digits to represent in memory which are 1 and 0
- lets say in colours we use three colours red green and blue each can either be on or off 
- thats a total of 8 combos because 2^3
- in the same way we say 256^3 now thats 16 million colours 
- we also get to know that a colour is representated by around 3 bytes or 24 bits
## hexadecimal
- it goes from 0 to 9 and a to f
- here each letter can represent a combo of 4 bits or a nibble 
## Color representation
- a colour is representated by 3 bytes or 24 bits
- each byte can represent 256 different values 
- each of the three bytes represent the intensity of the three red green and blue colours
- every 4 bits are representated by a hexadecimal digit

# conversion
- humans prefer the decimal system due to their 10 fingers
- computers prefer binary due to their high and low voltage 
- The binary number `1001` can be expressed as follows: 1001 = 1 × 23 + 0 × 22 + 0 × 21 + 1 × 20 = 1 × 8 + 0 × 4 + 0 × 2 + 1 × 1 = 8 + 0 + 0 + 1 = 9
- 
